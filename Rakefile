namespace :bump do
  desc "Increment the major level version number of the given package"
  task :major do

  end

  desc "Increment the minor level version number of the given package"
  task :minor do

  end

  desc "Increment the patch level version number of the given package"
  task :patch do

  end
end

desc "Tag and publish the package"
task :release do

end

